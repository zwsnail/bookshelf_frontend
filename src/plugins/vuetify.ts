import Vue from 'vue';
import Vuetify from 'vuetify/lib';

import 'vuetify/dist/vuetify.min.css'

Vue.use(Vuetify);

// //增加一个alert全局
// import {V-alert} from 'vuetify/lib';
// Vue.prototype.$alter = V-alert;
export default new Vuetify({
});
